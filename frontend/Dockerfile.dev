# Dockerfile for Frontend Development Environment
FROM node:20

WORKDIR /app

COPY package*.json ./
RUN npm install

COPY . .

# Expose Vite's default port
EXPOSE 5173

# Enable hot reload & access from host machine
CMD ["npm", "run", "dev", "--", "--host"]
